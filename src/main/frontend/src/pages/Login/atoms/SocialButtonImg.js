import styled from "@emotion/styled";

const SocialButtonImg = styled.img`
  position: absolute;
  height: auto;
  top: 50%;
  left: 15px;
  widrh: 32px;
  border: none;
  transform: translateY(-50%);
`;

export default SocialButtonImg;
